## 보고 형식 (기획 보고서)

- **보고일시**: 2025-12-19 (금) 23:41:22
- **보고자**: PM(Assistant)
- **보고 대상**: 사장님

## 1) 결론 요약 (1~3줄)
- “지표/통계(Stats)”는 외부 플랫폼 의존도가 높아 리스크가 있으므로, **1차는 내부 지표(우리 서비스 내)부터 시작**하고, 외부 지표(YouTube/SoundCloud)는 **2차 옵션**으로 분리하는 안을 추천드립니다.
- `tracks.metadata.stats` 구조는 이미 확보되어 있으니, 이번 결재는 **“무슨 지표를 어떤 키로 관리할지”**만 확정하면 됩니다.

## 2) 서비스 목적/컨셉 정리
- **목적**: YouTube + SoundCloud 기반 곡 아카이브 + 곡 단위 감상(일기형 코멘트)
- **원칙**: 지표/통계는 확장 여지만 확보하고, 운영 리스크(정책/토큰/레이트리밋)를 통제한다.

## 3) 현재 파악된 현황(팩트)
- 기본 메타(제목/업로더/길이/썸네일/링크/추가일시)는 구현 완료.
- 확장 슬롯: `tracks.metadata` 및 `tracks.metadata.stats`에 지표 적재 가능 구조는 확보.

## 4) 이슈/리스크
- **외부 지표 수집 리스크**: 정책/토큰/레이트리밋/플랫폼별 제공 범위 차이로 “지표가 갑자기 사라짐” 가능.
- **표시 신뢰도 리스크**: 마지막 동기화 시각(`last_synced_at`) 없으면 사용자 오해 가능.

## 5) 결정 필요(사장님 결재 요청)
- **안건 1: 1차(즉시) 지표 범위 선택**
  1) **추천: 내부 지표만** (외부 API 불필요)
     - `comment_count`(우리 DB), `added_at`(이미 존재), `last_synced_at`(null)
  2) 내부 + YouTube 외부 지표(조회/좋아요/댓글) 포함(YouTube Data API 키 필요)
  3) 내부 + YouTube + SoundCloud 외부 지표(플랫폼별 정책/키 이슈 큼)
  - **추천**: 1

- **안건 2: stats 키 확정(초기 키셋)**
  1) **추천: 최소 키셋**
     - 공통: `comment_count`, `last_synced_at`
     - YouTube: `views`, `likes`, `comments`
     - SoundCloud: `plays`, `likes`, `comments`
  2) 확장 키셋(공유/저장 수 등까지 확장)
  - **추천**: 1

## 6) 다음 작업(내가 진행)
- 사장님 결재 결과를 `agents/planning/tickets/T-003__track_stats_metrics_spec.md`에 append로 고정
- DEV에게 착수 가능한 티켓 형태로 넘김(구현은 DEV)

## 7) 산출물/링크
- 티켓: `agents/planning/tickets/T-003__track_stats_metrics_spec.md`
- 관련 백로그: `agents/planning/04_BACKLOG.md`








